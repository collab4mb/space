#!/bin/sh

if [ ! -d "build" ]; then
  mkdir build
fi

cd build

../sokol/shdc/linux/sokol-shdc --input ../shaders.glsl --output shaders.glsl.h --slang glsl330

gcc ../main.c -I/usr/include/X11/extensions -g -lX11 -lXi -lXcursor -lGL -lasound -ldl -lm -lpthread -fsanitize=address

